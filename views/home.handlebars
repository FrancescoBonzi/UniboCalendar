<div class="container">
  <div class="header">
    <h1>Unibo Calendar</h1>
    <h5>Sleziona il tuo corso di studi, l'anno che stai frequentando e il curriculum.</h5>
  </div>
  <div class="container">
    <form id="form_calendar" class="form-group" action="/course" method="post" onchange="checkFormValidity();">
      <div class="row">
        <select id="areas" class="form-control" name="areas" onchange="getCoursesGivenArea();">
          <option value="">--- Seleziona Scuola ---</option>
          {{#each areas}}
          <option value="{{this}}">{{this}}</option>
          {{/each}}
        </select>
      </div>
      <div class="row">
        <select id="courses" class="form-control" name="courses" onchange="getYearsAndCurriculaGivenCourse();">
        </select>
      </div>
      <div class="row">
        <select id="years" class="form-control" name="years">
        </select>
      </div>
      <div class="row">
        <select id="curricula" class="form-control" name="curricula">
        </select>
      </div>
      <div class="row">
        <input type="submit" id="submit-form-button" class="btn btn-primary" value="Avanti" disabled>
      </div>
    </form>
  </div>
</div>